{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (CMake Tools active target)",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
