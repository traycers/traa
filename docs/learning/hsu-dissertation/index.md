# Конспект: Aaron W. Hsu, «A Data Parallel Compiler Hosted on the GPU»

## Библиография

**Aaron W. Hsu. *A data parallel compiler hosted on the GPU*.** PhD dissertation, Indiana University, School of Informatics, Computing, and Engineering, ноябрь 2019. 284 стр.

Постоянная ссылка: [hdl.handle.net/2022/24749](https://hdl.handle.net/2022/24749) · [прямой PDF](https://scholarworks.iu.edu/dspace/bitstreams/dcbd5240-8454-4533-bc0c-ac3ee7628b8e/download) (29 МБ) · [издание на Leanpub](https://leanpub.com/arcfide_dissertation).

## О чём диссертация

Автор строит полный компилятор языка **Co-dfns** (лексически скопированный, динамически типизированный APL-диалект в синтаксисе dfns) и реализует **все проходы компилятора без ветвлений, без рекурсии и без ADT/record-абстракций** — только композиция стандартных array-примитивов APL над деревом, представленным как Structure-of-Arrays (SoA). Итоговый компилятор умещается в **17 строк APL-кода** против ~1000 строк эталонной реализации на фреймворке Nanopass (Racket/Chez Scheme), при этом на CPU обгоняет её в среднем в **~33 раза**, а на GPU — в **~200 раз**, используя на порядок меньше памяти под AST.

Главный тезис: непригодность деревьев для параллельных архитектур (SIMD CPU, GPU) — не свойство самих трансформаций дерева, а следствие традиционного выбора абстракций данных (указатель-ориентированные record-типы). Замена этой абстракции на плоские векторы (parent vector + производные колонки) и работа с деревом как с обычным array-данными снимает проблему почти без потерь в простоте кода.

## Связь со справочником по деревьям на базе массивов

Справочник по самим структурам данных (path coordinate matrix, propagation, DFS/BFS, LCA, изменение дерева) уже есть — [`../array-trees/`](../array-trees/index.md). Этот конспект **не дублирует** тот справочник, а покрывает диссертацию целиком: контекст APL/array-программирования, все 11 проходов компилятора как таковые (не только структуру AST), бенчмарки производительности и итоговые выводы/ограничения автора.

## Содержание

| Документ | О чём |
|----------|-------|
| [1. APL, Co-dfns и Nanopass](01-apl-and-background.md) | Array-модель данных, синтаксис APL, идиоматичный стиль, язык Co-dfns (dfns/guards), фреймворк Nanopass как эталон сравнения |
| [2. Архитектура компилятора и представление AST](02-ast-representation.md) | 11 проходов компилятора, почему отвергнуты record-type/depth-vector/path-matrix, вычисление parent vector, вычисление лексического контура |
| [3. Проходы компилятора 3.5–3.13](03-compiler-passes.md) | Lifting functions, wrapping expressions, guard tests, rank counting, flattening, frame slots, лексический стек, экспорт имён, лексическое разрешение |
| [4. Производительность](04-performance.md) | Методология бенчмарков, память AST, overhead передачи CPU↔GPU, CPU vs GPU, сравнение с Racket Nanopass и Chez Scheme |
| [5. Обсуждение: идиомы, преимущества, ограничения](05-discussion-idioms.md) | Traversal/Edge Mutation/Node Mutation, аргументы за подход, честно признанные ограничения, вопрос читаемости APL |
| [6. Future Work, Related Work, Conclusion](06-future-related-conclusion.md) | Направления развития, сопоставление с другими работами, итоговый вклад диссертации |

## Терминология (сквозная по всем файлам)

- **dfns** — синтаксис анонимных функций APL (John Scholes), используемый как единственная форма определения функций во входном языке Co-dfns.
- **Guard** (`тест : следствие`) — единственная условная конструкция dfns; **Error Guard** (`::`) — перехват ошибок по кодам.
- **Parent vector** `p` — `p[i]` = id родителя узла `i`; корень указывает сам на себя (`p[i]=i`).
- **Referent** `r` — id ближайшего охватывающего узла-функции (лексический контур) для каждого узла.
- **Traversal idiom** — идиома «шаг вверх по `p` до предиката, до неподвижной точки» (`⍣≡`), основной инструмент bottom-up обхода в компиляторе.
- **SoA (Structure of Arrays)** — представление AST как набора параллельных векторов-колонок (`p`, `t` — type, `k` — kind, `n` — name/node-data, `r`), а не как записей-объектов с указателями.
